# Challenge Findings — 003-launch-cost-curve-value-migration (2026-09-19)

**Backstops triggered**: none (manual run)
**Deterministic scan**: 0 finding(s), 0 dropped (Q40 cap)

## Deterministic findings (content-derived IDs — re-runs byte-identical)

- (none)

## IC findings (append the analyst's judgment angles: pre-mortem / inversion / wrong_if falsifiability review)

### C-1 — **[HIGH] The cross-run contradiction scan is UNEXERCISED, not clean.**

The deterministic scan returned `contradictions: 0` over 45 artifacts. That number was
**arithmetically forced**, and reading it as a pass would be the error this thesis spent its
length documenting. `reduce_journals.build_entity_index` groups on
`(entity, metric, period)` read from each artifact's `entity_claims` frontmatter. Measured:

| Quantity | Value |
|---|---|
| Artifacts scanned | **45** |
| Files declaring `entity_claims` | **0** |
| Claims with a value | **0** |
| Distinct `(entity, metric, period)` keys | **0** |

`entity_claims` is **not a field in `contracts/artifact-frontmatter.yaml`**, so no artifact can
carry one and `metrics.json` records `[]` for all nine tickers. The skill's headline capability
— cross-run contradiction — **cannot fire on this thesis by construction.**

This is the thesis's own class arriving in its own verification: `REACHABLE-BUT-NOT-RECORDABLE`.
No amount of further research fixes it; the contract needs an admissible field. **A check that
cannot fail is not a passing check** (003 §A4, in its own words).

### C-2 — **[HIGH] The Electron inversion rests on two integers, and the sensitivity is unstated.**

The report's headline — cost per launch **−12.0%** against cost per kg **+32.0%** — is
`0.88 × 1.50`. Both factors come from **two counts in one quarter**: 6 missions, of which 2 were
HASTE suborbital testbeds carrying 0 kg. The corrected denominator is 4.

**Sensitivity, which the report does not state.** The identity is
`cost-per-kg ratio = (cost/launch ratio) × (mission-count ratio)`, so:

| Orbital missions | `0.88 ×` | Headline | |
|---|---|---|---|
| 3 | 2.00 | **+76.0%** | |
| **4** | 1.50 | **+32.0%** | ← the report's claim |
| 5 | 1.20 | **+5.6%** | |
| 6 | 1.00 | **−12.0%** | ← the *uncorrected* reading the report criticises |

**One mission reclassified from HASTE to orbital moves the headline from +32.0% to +5.6% — the
claimed effect shrinks by 5.7×.** And if all six were orbital the metric turns *negative*, which
is precisely the uncorrected reading the report exists to reject. The claim is therefore
**knife-edge on two small integers read from one quarter**, while the report presents it as the
thesis.

**What would have to be wrong:** the HASTE classification of one mission, or the quarter's
mission count. Both are filed, but neither carries a stated tolerance, and no second quarter is
shown to corroborate the direction. **Pre-mortem path:** the loss began here — a reader acted on
a +32% inversion that was really a mission-mix artefact of Q2 2026.

### C-3 — **[HIGH] The report identifies the winner and declines to act on it.**

PIL-2's verdict and the value-pool table say the value sits with **whoever owns the demand** —
and **SPCX Connectivity carries +38.59% operating margin while owning the subscriber**: the
highest-margin, best-positioned asset in the universe on the thesis's own criterion.

The report closes: *"The recommendation is not a trade. This thesis produces no positions by
design."* That is a defensible scope choice, but it means the thesis's own logic terminates in a
conclusion it refuses to state. **Inversion:** if the value-pool finding is right, the natural
trade is long the demand-owning integrator — and the report declines to say so, so the finding
cannot be scored either way. **A thesis that no position can falsify is not yet an investment
thesis**; it is a research note with an investment-shaped conclusion.

### C-4 — **[MEDIUM] The price evidence for "margin is non-informative" is n = 2.**

A3 rests on two acquisitions twelve months apart: GSAT at ≈41.4× TTM revenue on a **−7.37%**
margin, IRDM at ≈8.3× on **+15.10%** — a ≈5.0× multiple ratio against a 22.5-point margin
differential. The report reads this as the market pricing demand, not margin.

**Two observations cannot establish a pricing rule.** They are also not independent in the way
the framing implies: one buyer is a hyperscaler acquiring spectrum, the other is a launcher
acquiring a customer base — the *strategic logics differ*, so the multiple gap may reflect two
different kinds of transaction rather than one market verdict delivered twice. **What would
falsify it:** a third acquisition in the universe where the multiple tracks margin. n = 2 is a
coincidence and a signal with equal claim to the data.

### C-5 — **[MEDIUM] Every falsifier reads a source the thesis declares defective.**

All six `wrong_if` conditions take `source=issuer_filing*` or `RKLB_10-Q_*` — i.e. the platform's
served filing layer. The same thesis records **DA-23 sign-stripping at 9 of 9 tickers**, and its
sharpest instance is *an artifact written to document DA-23 itself reproducing it*. So the
falsifiers are mechanised against a layer this thesis says is unreliable **unless every value is
recomputed from components first**. No falsifier states that precondition.

This is not a defect in any single falsifier; it is a **shared dependency** across the set, and
it means a future clean-looking falsifier evaluation carries the DA-23 exposure silently.

### C-6 — **[MEDIUM] `wrong_if` review: two of six are specified but not evaluable.**

Assessed against Q8-4 (metric + threshold + source + op). All six are **mechanically specified** —
no prose falsifiers. Evaluability differs:

| Pillar | Specified | Evaluable | Note |
|---|---|---|---|
| PIL-1 | ✓ | ✓ but **n = 1** | threshold 0, `op=>`: cannot fire on a population with one demonstrated vehicle unless that one fails |
| PIL-2 | ✓ | ✓ | evaluated; HOLDS |
| PIL-3 | ✓ | ✓ | share ≥ 0.5 of claims with a filed basis |
| PIL-4 | ✓ | **✗ void as originally scored** | the threshold was **corrected 2939 → 5567 by this thesis**; the original evaluation is void, and the corrected one is knife-edge on an A′ payload that has never flown |
| PIL-5 | ✓ | ✓ | gap > 5% at RKLB |
| PIL-6 | ✓ | **✗ at 3 of 4 names** | `threshold=0.10` over the demand-side names — but the thesis finds the metric **NON-FORMABLE at three of four**. Specified, not evaluable there |

**PIL-4 and PIL-6 are the load-bearing cases**: a falsifier whose threshold moved during the
thesis, and one whose metric cannot be constructed at 75% of its own population. Both are being
reported as verdicts rather than as unexercised tests.

---

*IC angles written 2026-09-19. The deterministic section above is machine-generated and re-runs
byte-identically; this section is analyst judgment and carries no content-derived ID.*

